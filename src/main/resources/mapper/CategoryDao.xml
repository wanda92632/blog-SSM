<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.wanda92632.blog.dao.CategoryDao">
    <select id="getAllCategory" resultType="top.wanda92632.blog.entity.Category">
        SELECT
            *
        FROM
            category
        LIMIT 1,8
    </select>
</mapper>